actions:
  - name: triggerWorkflowRun
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/actions/trigger-workflow-run'
    permissions:
      - role: owner
      - role: editor
    comment: Starts a run for a workflow the caller may trigger in their own organization
custom_types:
  enums: []
  input_objects: []
  objects:
    - name: TriggerWorkflowRunOutput
  scalars: []
